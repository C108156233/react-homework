import React from 'react';
import ReactDOM from 'reacr-dom';
import app from './app';

ReactDOM.render(
    <React.StrictMode>
        <app />
        </React.StrictMode>
        document.getElementById('root')
);